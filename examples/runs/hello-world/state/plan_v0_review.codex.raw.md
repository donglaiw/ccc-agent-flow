/codex:adversarial-review --wait Review examples/runs/hello-world/artifacts/plan_v0.md against examples/runs/hello-world/task.md.

Illustrative inline Codex output, copied before CCC normalization:

The plan is clear but should state whether the command has CLI wiring. It names a module but does not identify how users execute it.
